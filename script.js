/**
 * Main JavaScript file for Andy Wong's Portfolio
 * Currently handles basic initialization.
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Portfolio successfully loaded.');
    
    // Future JavaScript logic (e.g., mobile menu toggling, dynamic data fetching, 
    // or intersection observers for scroll animations) can be added here.
});